# OTT Telegram Bot

A Telegram bot for managing OTT subscription sales.

## Setup
1. Add your bot token in `config/settings.py`.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
